DECLARE
	 CURSOR C1 IS 
	 	SELECT E.EMPNO , E.ENAME, D.DNAME, D.LOC
		FROM EMP E, DEPT D 
		WHERE E.DEPTNO = D.DEPTNO AND E.EMPNO = &VEMPNO;

BEGIN
    FOR C2 IN C1 LOOP
	DBMS_OUTPUT.PUT_LINE(C2.EMPNO || ' ' ||C2.DNAME||' '||C2.LOC);
	END LOOP;


END;
/ 


